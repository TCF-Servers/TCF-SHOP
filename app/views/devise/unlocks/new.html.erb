<div class="auth-heading">
  <h2>Déverrouiller le compte</h2>
  <p>Entrez votre email pour recevoir à nouveau les instructions de déverrouillage.</p>
</div>

<%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "auth-form" }) do |f| %>
  <%= f.error_notification %>
  <%= f.full_error :unlock_token %>

  <%= f.input :email,
              label: "Email",
              required: true,
              autofocus: true,
              input_html: { autocomplete: "email" } %>

  <div class="auth-actions">
    <%= f.button :submit, "Renvoyer les instructions", class: "btn auth-btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
